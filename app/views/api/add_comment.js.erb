<%= juvia_handle_response(
  :action    => 'UpdateTopic',
  :site_key  => @site_key,
  :topic_key => @topic_key,
  :html      => render(:partial => 'api/topic.html', :locals => { :topic => @topic })
) %>
