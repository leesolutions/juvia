<%= {
  :status    => 'ok',
  :action    => 'UpdateTopic',
  :site_key  => @site_key,
  :topic_key => @topic_key,
  :html      => render(:partial => 'api/topic.html', :locals => { :topic => @topic })
}.to_json.html_safe %>
